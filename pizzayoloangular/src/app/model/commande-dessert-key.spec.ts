import { CommandeDessertKey } from './commande-dessert-key';

describe('CommandeDessertKey', () => {
  it('should create an instance', () => {
    expect(new CommandeDessertKey()).toBeTruthy();
  });
});
